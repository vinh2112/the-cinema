export const movieState$ = (state) => state.movies;

export const tvState$ = (state) => state.tvshows;

export const searchState$ = (state) => state.search;

export const filterState$ = (state) => state.filter;

export const modalState$ = (state) => state.modal;
